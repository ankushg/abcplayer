/**
 * Package containing all of our test files. One component of our testing strategy is to
 * partition the spaces in the test for each class, but try to make our objects within
 * those partitions public and static. This allows us to reuse them in tests for
 * components higher in the hierarchy.
 */
package tests;

